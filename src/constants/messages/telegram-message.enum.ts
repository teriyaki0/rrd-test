enum COMMON {
  DESCRIPTION = "Open Mini App",
}

enum CHAT_BUTTON_INFO {
  TYPE = "web_app",
  TEXT = "text",
}

enum COMMANDS {
  START = "start",
}

export const TELEGRAM = {
  COMMON,
  CHAT_BUTTON_INFO,
  COMMANDS,
};
