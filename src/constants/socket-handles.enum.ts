enum COMMON {
  CONNECTION = "connection",
  DISCONNECT = "disconnect",
  ERROR = "error",
}

enum DOUBLE {
  DOUBLE = "double",
  ERROR = "doubleError",
  RESULT = "doubleResult",
}

export const SOCKET_HANDLES = { COMMON, DOUBLE };
